#!/bin/bash
ifa=0; ofa=0; pfa=0;Cfa=0; nfa=0;
 if=1; iv=$OPTAR;
  ofa=1; ov=$OPTAR;
    pfa=1; pv=$ORTAR;
       Cfa=1;
	 nf=1;
	    echo illegal option $optletter
	    
	      if (($pfa==0))
	      then echo  "шаблон не найдет"
	      else
		  if (($ifa==0))
		  then echo "файл не найден"
		  else
		      if(( $Cfa==0))
		      if (($Cfa==0))
			   then grep $pv $iv
			   else grep -n $pv $iv
			   fi
			   else if (($nfa==0))
				then grep -i $pv
				else grep -i -n $pb $iv
				fi
		      fi
	      
			   fi
			   else if (($nfa ==0))
				then qrep -i $pv $iv > $ov
				     
				else grep -i -n $pv $ib > $ov 
				fi
		       fi
		       fi
		       fi

		       fi
			   
					 

  
